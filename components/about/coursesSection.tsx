import { NextComponentType } from "next";
import styles from "./coursesSection.module.scss";

interface Props {}

const CoursesSection: NextComponentType<Props> = ({}) => {
    return <section>coursesSection  </section>;
};

export default CoursesSection;
