import { NextComponentType } from "next";
import styles from "./skillsSection.module.scss";

interface Props {}

const SkillsSection: NextComponentType<Props> = ({}) => {
    return <section>skillsSection </section>;
};

export default SkillsSection;
