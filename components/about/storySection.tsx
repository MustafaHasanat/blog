import { NextComponentType } from "next";
import styles from "./storySection.module.scss";

interface Props {}

const StorySection: NextComponentType<Props> = ({}) => {
    return <section>storySection </section>;
};

export default StorySection;
