import { NextComponentType } from "next";
import styles from "./languagesSection.module.scss";

interface Props {}

const LanguagesSection: NextComponentType<Props> = ({}) => {
    return <section>languagesSection </section>;
};

export default LanguagesSection;
