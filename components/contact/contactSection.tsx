import { NextComponentType } from "next";
import styles from "./contactSection.module.scss";

interface Props {}

const ContactSection: NextComponentType<Props> = ({}) => {
    return <section>contactSection </section>;
};

export default ContactSection;
