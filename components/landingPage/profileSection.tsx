import { NextComponentType } from "next";

interface Props {}

const ProfileSection: NextComponentType<Props> = ({}) => {
    return <section>profileSection </section>;
};

export default ProfileSection;
