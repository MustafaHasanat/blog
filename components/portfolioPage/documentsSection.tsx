import { NextComponentType } from "next";
import styles from "./documentsSection.module.scss";

interface Props {}

const DocumentsSection: NextComponentType<Props> = ({}) => {
    return <section>documentsSection </section>;
};

export default DocumentsSection;
