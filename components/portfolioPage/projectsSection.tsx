import { NextComponentType } from "next";
import styles from "./projectsSection.module.scss";

interface Props {}

const ProjectsSection: NextComponentType<Props> = ({}) => {
    return <section>projectsSection </section>;
};

export default ProjectsSection;
