import { NextComponentType } from "next";
import styles from "./servicesSection.module.scss";

interface Props {}

const ServicesSection: NextComponentType<Props> = ({}) => {
    return <section>servicesSection </section>;
};

export default ServicesSection;
